export const environment = {
  production: true,
  // After Render deploy, replace with your real URL if not using Vercel rewrites:
  // apiUrl: 'https://skillgraph-api.onrender.com'
  // With vercel.json rewrites (recommended), leave empty so requests go to /api on same origin:
  apiUrl: ''
};
